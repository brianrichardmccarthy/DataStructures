package stack;

public class Driver {

    public static void main(String[] args) {

        /*VectorStack<Integer> one = new VectorStack<>(2);
        one.push(1);
        one.push(2);
        one.pop();
        one.push(3);
        System.out.println(
            "One is full <" + one.isFull() + "> One is empty <" + one.isEmpty() + "> .toString <" + one.toString() + "> .pop <" + one.pop() + "> .peek <" + one.peek() + ">");*/
    }

}
